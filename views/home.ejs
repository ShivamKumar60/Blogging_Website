<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('./partials/head') %>
    <title>Homepage</title>
    <style>
      .heading{
        display: flex;
        justify-content: center;
        align-items: center;
        padding: auto;
        margin: 50px;
        color: white;
        flex-direction: column;
       
      }
      h2:hover{
        letter-spacing: 5px;
        transition-duration: 1s;
      }
      h2{
        font-family: Arial, Helvetica, sans-serif;
        font-weight: bold;
        font-size: 70px;
      }
      .card{
        border: 0px;
      }
      .card:hover{
        box-shadow: 0px 0px 5px rgba(255, 255, 255, 1);
        cursor: pointer;
        transform: translateY(-20px);
        transition-duration: 0.5s;
      }
      body{
        background: linear-gradient(to bottom, #000000, rgb(33, 31, 31));
        height: 100vh;
        width: 100%;
      }
      .desc{
        width: 50%;
        align-items: center;
        justify-items: center;
        display: flex;
        text-align: center;
      }
      .bgblack{
        background-color: #222222;
      }
      .button{
        background-color: #000000;
        color: white;
        border-radius: 10px;
      }
    </style>
  </head>
  <body class="bg">
    <%- include('./partials/nav') %>
    <div class="heading">
      <h2>Welcome to Blogify!</h2>
      <p class="desc">Explore the world with our captivating blog website! Immerse yourself in a world of insightful articles, engaging stories, and practical tips covering a wide range of topics. </p>
    </div>
    <div class="container mt-3">
      <div class="row row-cols-4">
        <% blogs.forEach(blog => { %>
        <div class="col">
          <div class="card col card" style="width: 18rem">
            <img src="<%= blog.coverImageURL %>" class="card-img-top" />
            <div class="card-body">
              <h5 class="card-title"><%= blog.title %></h5>
              <a href="/blog/<%= blog._id %>" class="btn button">View</a>
            </div>
          </div>
        </div>
        <% }) %>
      </div>
    </div>

    <%- include('./partials/scripts') %>
  </body>
</html>